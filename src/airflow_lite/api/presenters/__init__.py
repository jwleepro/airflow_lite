"""Presentation layer that assembles data for WebUI renderers.

Routes should delegate data gathering/transformation to presenters so
that renderers receive ready-to-display view models / dicts and route
handlers stay focused on HTTP concerns.
"""
