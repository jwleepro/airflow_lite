interface:
  display_name: "Project Bootstrap"
  short_description: "Start from AGENTS.md and current repo state"
  default_prompt: "Use $project-bootstrap to summarize the current repo state from AGENTS.md, the active request, and nearby context before doing work."
