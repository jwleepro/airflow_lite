interface:
  display_name: "GitHub Issue Cycle"
  short_description: "Milestone-ordered issue delivery with In Progress sync and per-issue worktrees"
  default_prompt: "Use $github-issue-cycle with github-issue-cycle-agent to pick the next milestone-ordered issue, claim it immediately by adding status:in-progress as the first blocking action (stop if claim fails), sync project Status to In Progress when possible with failure logging, execute all coding in a dedicated per-issue git worktree, open a draft PR, and close the loop after CodeRabbit feedback."
